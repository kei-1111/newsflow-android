package io.github.kei_1111.newsflow.android.feature.search

object SearchTestTags {
    object SearchScreen {
        const val Root = "SearchScreen"
    }

    object TopAppBar {
        const val Root = "SearchTopAppBar"
        const val BackButton = "SearchTopAppBarBackButton"
        const val OptionButton = "SearchTopAppBarOptionButton"
    }

    object SearchTextField {
        const val Root = "SearchTextField"
        const val ClearButton = "SearchTextFieldClearButton"
    }

    object Content {
        const val Root = "SearchContent"
    }

    object ArticleList {
        const val Root = "SearchArticleList"
    }

    object Loading {
        const val Root = "SearchLoading"
    }

    object OptionBottomSheet {
        const val Root = "SearchOptionBottomSheet"
    }
}
